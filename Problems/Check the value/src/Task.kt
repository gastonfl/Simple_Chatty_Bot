import java.util.Scanner

fun main(args: Array<String>) {
    val scanner = Scanner(System.`in`).nextInt()
    if (scanner > 0 && scanner < 10) {
    println(message = true)
    }else{
        println(message = false)

    }
    }
